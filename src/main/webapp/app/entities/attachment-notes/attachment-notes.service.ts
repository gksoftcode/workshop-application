import axios from 'axios';

import buildPaginationQueryOpts from '@/shared/sort/sorts';

import { IAttachmentNotes } from '@/shared/model/attachment-notes.model';

const baseApiUrl = 'api/attachment-notes';

export default class AttachmentNotesService {
  public find(id: number): Promise<IAttachmentNotes> {
    return new Promise<IAttachmentNotes>((resolve, reject) => {
      axios
        .get(`${baseApiUrl}/${id}`)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public retrieve(paginationQuery?: any): Promise<any> {
    return new Promise<any>((resolve, reject) => {
      axios
        .get(baseApiUrl + `?${buildPaginationQueryOpts(paginationQuery)}`)
        .then(res => {
          resolve(res);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public delete(id: number): Promise<any> {
    return new Promise<any>((resolve, reject) => {
      axios
        .delete(`${baseApiUrl}/${id}`)
        .then(res => {
          resolve(res);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public create(entity: IAttachmentNotes): Promise<IAttachmentNotes> {
    return new Promise<IAttachmentNotes>((resolve, reject) => {
      axios
        .post(`${baseApiUrl}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public update(entity: IAttachmentNotes): Promise<IAttachmentNotes> {
    return new Promise<IAttachmentNotes>((resolve, reject) => {
      axios
        .put(`${baseApiUrl}/${entity.id}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public partialUpdate(entity: IAttachmentNotes): Promise<IAttachmentNotes> {
    return new Promise<IAttachmentNotes>((resolve, reject) => {
      axios
        .patch(`${baseApiUrl}/${entity.id}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }
}
